## 1. Tokens e camadas

- [x] 1.1 Adicionar o token público de conteúdo de superfície selecionada nos dois temas e regenerar os metadados TypeScript, verificando os valores gerados.
- [x] 1.2 Aplicar o novo token a `AdsNav` e `AdsTabs`, e posicionar `AdsTooltip` e `AdsPopover` na camada de overlay, verificando por testes de componente.

## 2. Higiene e validação

- [x] 2.1 Remover o artefato `debug.log` e ignorar sua recorrência, verificando que o arquivo não está mais rastreado.
- [x] 2.2 Formatar os arquivos alterados e executar testes de componentes, validação estrita OpenSpec e checagem de formatação.
