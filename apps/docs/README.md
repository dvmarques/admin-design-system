# Documentação visual

`apps/docs` executa o Storybook e consome somente as APIs públicas de
`@admin-ds/tokens` e `@admin-ds/components`.

```powershell
npm.cmd run dev --workspace @admin-ds/docs
```

O catálogo inicia em `http://localhost:6006` e permite alternar entre os
temas claro e escuro pela barra de ferramentas.
